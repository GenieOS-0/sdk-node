name: Release

# Tag-driven publish for @genie-os/sdk.
#
#   git tag v0.1.1
#   git push origin v0.1.1
#
# The tag value should match the `version` field in package.json. Provenance
# attestations require this repo to be public.

on:
  push:
    tags: ['v*']

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write    # required for npm provenance
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      # npm Trusted Publishing (OIDC) requires npm >= 11.5.0; the runner ships older.
      - run: npm install -g npm@latest

      - name: Verify tag matches package.json version
        run: |
          PKG_VERSION=$(node -p "require('./package.json').version")
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "::error::Tag v$TAG_VERSION does not match package.json v$PKG_VERSION"
            exit 1
          fi

      # Use `npm install`, not `npm ci`: the committed lockfile would pin only
      # the host platform's optional deps, and rollup's per-platform native
      # binary (pulled in by tsup --dts) then fails on the Linux runner
      # (npm/cli#4828). `npm install` re-resolves optional deps per platform.
      - run: npm install --no-audit --no-fund
      - run: npm run build
      - run: npm test --if-present

      # Published via npm Trusted Publishing (OIDC) — no NPM_TOKEN needed.
      # Provenance is attached automatically from this GitHub Actions run.
      - run: npm publish --provenance --access public
